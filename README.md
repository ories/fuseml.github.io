# FuseML website (fuseml.github.io)

this repo is using the Hugo hugo-fresh theme, which is added as a submodule.

use:
    git clone THISREPO
    git submodule init
    git submodule update

to check out the whole tree, followed by `$> hugo server` to deploy locally.
